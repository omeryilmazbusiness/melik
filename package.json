{
  "name": "sirin-kids",
  "version": "1.0.0",
  "private": true,
  "description": "Şirin Kids — tek servis (Next.js + Express + PostgreSQL)",
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "railway:build": "npm install --prefix backend && npm install --prefix frontend && NEXT_PUBLIC_API_URL=/api npm run build --prefix frontend",
    "railway:start": "npm run db:migrate --prefix backend && node scripts/railway-start.mjs",
    "db:setup": "npm run db:setup --prefix backend",
    "db:seed": "npm run db:seed --prefix backend",
    "db:migrate": "npm run db:migrate --prefix backend",
    "build": "npm run railway:build",
    "start": "npm run railway:start"
  },
  "engines": {
    "node": ">=20"
  }
}
